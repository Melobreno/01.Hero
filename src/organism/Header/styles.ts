import styled from "styled-components";

export const box = styled.header`
    padding: 30px 0;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
`